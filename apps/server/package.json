{
  "name": "@ravens/server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ravens/contracts": "workspace:*",
    "@ravens/game-engine": "workspace:*",
    "@node-rs/argon2": "latest",
    "drizzle-orm": "latest",
    "fastify": "latest",
    "@fastify/cookie": "latest",
    "@fastify/websocket": "latest",
    "pg": "latest"
  },
  "devDependencies": {
    "@types/pg": "latest"
  }
}
